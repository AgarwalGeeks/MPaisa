apiVersion: v1
kind: Secret
metadata:
  name: mpaisa-secrets
type: Opaque
stringData:
  # Example values for local or test clusters; in production prefer AWS Secrets Manager + External Secrets.
  DB_SOURCE: "postgresql://user:password@rds-endpoint:5432/MPaisa?sslmode=require"
  PASETO_SYMMETRIC_KEY: "change-this-to-a-32-byte-random-string"

